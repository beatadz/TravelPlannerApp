import { Pressable, StyleSheet, Text, View, Image } from "react-native";

const Overview = () => {
	return <View style={styles.container}>{/* <Text>Overview</Text> */}</View>;
};

const styles = StyleSheet.create({
	container: {
		backgroundColor: "#FFFFFF70",
	},
});

export default Overview;
